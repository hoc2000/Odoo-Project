<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- QWeb Reports -->
    <!-- NAME & FILE_NAME should be the same -->
    <record id="report_tikcet_details" model="ir.actions.report">
        <field name="name">Ticket Report</field>
        <field name="model">mockdesk.ticket</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">mock_desk.report_ticket</field>
        <field name="report_file">mock_desk.report_ticket</field>
        <field name="binding_type">report</field>
    </record>
</odoo>